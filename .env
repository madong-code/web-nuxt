# 环境变量说明
# Nuxt 4 使用以下文件管理环境变量：
#   .env.development  - 开发环境（nuxt dev 时自动加载）
#   .env.production   - 生产环境（nuxt build 时自动加载）
#   .env              - 公共变量（始终加载，可被上述文件覆盖）
#
# 变量前缀说明：
#   NUXT_PUBLIC_ 前缀 - 暴露到客户端（可通过 useRuntimeConfig().public 访问）
#   无前缀          - 仅服务端可用

# 默认语言（可被 .env.development / .env.production 覆盖）
NUXT_PUBLIC_DEFAULT_LANG=zh-CN
